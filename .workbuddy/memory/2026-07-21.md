# 2026-07-21

- 部署婚礼管家新人登记端至 CloudStudio，生成分享链接并制作对应二维码。
- 分享链接：https://5e818174a720463e825ea3d39e812b2f.app.codebuddy.work
- 二维码文件：登记端二维码.png
- 根据Word问卷文档新增第7步"深度了解"（13个子问题），涵盖梦想婚礼描述、宾客构成、迎亲敬茶、摄影造型、主持风格、外景喜好、家庭情感等。同时更新了详情面板展示和CSV导出字段。
- 管家后台添加密码保护（默认密码8888），进入需验证，可锁定/修改密码，关闭浏览器后需重新验证
- 清空全部数据增加管理密码二次确认验证
- 解决"客人填写资料查收不到"问题：将存储从纯localStorage改为云端共享模式。本地Node.js服务器(server.js)提供REST API + JSON文件持久化，通过localtunnel隧道(zhouli-wedding.loca.lt)公网暴露。前端通过API读写数据，客户提交→云端保存→管家后台实时查看。创建了启动服务器.bat一键启动脚本。
- 新人端与管家端界面彻底分离：按角色显示不同底部tab（新人端：首页/登记/我的；管家端：首页/管家/我的），首页hero文案/CTA按角色切换，数据概览/快捷功能/最近登记仅管家端可见，我的页面菜单按角色过滤（新人端仅清空草稿）。角色持久化到sessionStorage。
- 管家端管理员权限系统：每次从新人端切换到管家端必须输入密码验证（同一会话中切tab无需重复）；添加管理员姓名/密码设置弹窗；我的页面显示登录状态标签（已登录/未登录）；退出管理员登录功能。
- 创建 Android APK 构建项目（apk-builder/）：基于 Capacitor + GitHub Actions，零本地 SDK 依赖，推送代码后云端自动编译签名APK。包含一键构建脚本一键构建APK.bat。
- 修复管家端点击无反应问题：密码弹窗取消按钮缺少id="pwdCancel"，导致showPwd函数执行document.getElementById('pwdCancel').style.display报错，阻塞了整个登录流程。修复后重新部署到CloudStudio。
- APK生成成功：下载JDK 17.0.19+10（190MB）+ apktool 2.9.3反编译模板APK→修改smali代码中的URL（https://example.com→CloudStudio地址）和APP名（WebToApk→周礼婚礼管家）→重打包→uber-apk-signer签名。因路径含中文导致aapt2/temp异常，通过-Djava.io.tmpdir="C:/temp"绕过。最终APK：C:\Users\周海红\WorkBuddy\Claw\周礼婚礼管家.apk（7.6MB）
- 将第1步"备选婚期"字段拆分为"新郎婚房地址"和"新娘婚房地址"两个文本输入框。同步更新了详情面板展示、CSV导出表头和数据行、示例数据。
- 将第2步"婚礼总预算"（chip选择）拆分为三个独立文本字段：餐标预算、五大人员预算（司仪/化妆/摄影/摄像/策划）、宴会布置预算。移除预算必填校验，改为至少填写桌数或宾客数。同步更新卡片摘要、详情面板、CSV导出、示例数据。
