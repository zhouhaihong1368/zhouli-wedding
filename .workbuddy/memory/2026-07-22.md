# 2026-07-22 工作日志

## 云端同步问题诊断与修复

**问题**：资料登记云端无法同步
**根因**：
1. 本地服务器(node server.js)未运行
2. localtunnel 子域名 `zhouli-wedding-2026` 被占用，隧道失效
3. 架构问题：前端在CloudStudio云端，后端在本地电脑+localtunnel，依赖本地电脑开机+隧道稳定

**临时修复**：
- 重启本地服务器(端口3000)
- 更换 localtunnel 子域名为 `zhouli-wedding-v3`
- 更新 index.html 中 API_BASE → `https://zhouli-wedding-v3.loca.lt`
- 重新部署到 CloudStudio (`5e818174a720463e825ea3d39e812b2f`)
- 更新 `启动服务器.bat` 中的子域名

**待做**：迁移到云数据库（永久方案），推荐 Supabase 免费层

## Render 云部署改造（方案B）

**目标**：将 server.js 部署到 Render 云端 + PostgreSQL，彻底告别本地依赖

**已完成**：
1. server.js 改为双模式：有 DATABASE_URL 用 PostgreSQL，无则用本地 JSON 文件
2. package.json 添加 pg 依赖，版本升至 2.0.0
3. 创建 .gitignore（排除 node_modules/data.json/apk-builder）
4. 创建 RENDER_DEPLOY.md 部署指南
5. localtunnel v3 子域名再次失效(503)，换成随机域名 `true-adults-start.loca.lt`
6. 重新部署到 CloudStudio，API_BASE 更新为新隧道地址
7. 启动服务器.bat 改为随机域名模式（不再指定固定子域名）

**问题修复**：客人提交后空白页 → localtunnel 503 导致 API 不通，重启隧道后恢复

**待做**：用户注册 GitHub → 推送代码 → Render 部署（永久方案）
- server.js 双模式已就绪，Render 部署后设置 DATABASE_URL 环境变量即可自动切换 PostgreSQL
- 用户尚未注册 GitHub 账号
